services:
  backend:
    build: ./backend
    container_name: taskflow-backend
    ports:
      - "4000:4000"
    env_file:
      - ./backend/.env

  frontend:
    build: ./frontend
    container_name: taskflow-frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend